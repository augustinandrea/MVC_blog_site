// Dependencies
const path = require("path");
const express = require("express");

// all routes asdefined in the controllers folder
const routes = require("./controllers/");
const sequelize = require("./config/connection");
